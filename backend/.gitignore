.venv/
__pycache__/
*.py[cod]
.env
*.db
.pytest_cache/
.ruff_cache/

# Feedback and counters written at runtime. The flagged photos are farmers'
# own images and the counters are per-deployment, so neither belongs in git.
data/flagged_diagnoses.jsonl
data/flagged_images/
data/usage_stats.json
